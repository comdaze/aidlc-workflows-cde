{
  "version": "v1",
  "hooks": [
    {
      "name": "aidlc-block",
      "trigger": "PreToolUse",
      "matcher": "fs_write|str_replace|fs_append|execute_bash",
      "description": "Hard-blocks MUTATING tool calls while an approval gate is open and no human acted since (human-presence floor). The matcher is the same mutation surface aidlc-audit-logger and aidlc-shell-post already recognise; a human-presence floor has no reason to gate a read, and without a matcher this fires on every tool call - measured at ~80ms of bun startup each, to reach a carve-out that returns 0 immediately under autonomous Construction or with no gate open.",
      "action": { "type": "command", "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts block" }
    }
  ]
}
